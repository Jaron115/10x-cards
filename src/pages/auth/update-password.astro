---
import { UpdatePasswordView } from "@/components/auth/UpdatePasswordView";
import Layout from "@/layouts/Layout.astro";
import { SUPABASE_URL, SUPABASE_KEY } from "astro:env/server";

export const prerender = false;

// Check if user has valid reset token
// Note: Token validation happens client-side using the token from URL hash
// This page is accessible without authentication but requires a valid reset token in URL
---

<Layout title="Nowe hasło - 10x-cards">
  <UpdatePasswordView client:load supabaseUrl={SUPABASE_URL} supabaseKey={SUPABASE_KEY} />
</Layout>
